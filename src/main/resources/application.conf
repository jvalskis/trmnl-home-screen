screen {
  templateFile = "screen.liquid"
  templateFile = ${?SCREEN_TEMPLATE_FILE}
  renderIntervalSeconds = 30
  renderIntervalSeconds = ${?SCREEN_RENDER_INTERVAL_SECONDS}
}

weather {
  enabled = true
  enabled = ${?WEATHER_ENABLED}
  apiKey = "YOUR_API_KEY"
  apiKey = ${?ACCUWEATHER_API_KEY}
  city = "London"
  city = ${?WEATHER_CITY}
  fetchIntervalMinutes = 60
  fetchIntervalMinutes = ${?WEATHER_FETCH_INTERVAL_MINUTES}
}

calendar {
  enabled = true
  enabled = ${?CALENDAR_ENABLED}
  calendarUrl = "https://nextcloud.example.com/remote.php/dav/calendars/user/personal/"
  calendarUrl = ${?CALDAV_CALENDAR_URL}
  authType = "basic"
  authType = ${?CALDAV_AUTH_TYPE}
  username = "user"
  username = ${?CALDAV_USERNAME}
  password = "app-password"
  password = ${?CALDAV_PASSWORD}
  fetchIntervalMinutes = 30
  fetchIntervalMinutes = ${?CALDAV_FETCH_INTERVAL_MINUTES}
  daysAhead = 7
  daysAhead = ${?CALDAV_DAYS_AHEAD}
}

homeAssistant {
  enabled = false
  enabled = ${?HOME_ASSISTANT_ENABLED}
  webSocketUrl = "wss://home-assistant.local/api/websocket"
  webSocketUrl = ${?HOME_ASSISTANT_WEBSOCKET_URL}
  accessToken = "YOUR_HA_TOKEN"
  accessToken = ${?HOME_ASSISTANT_ACCESS_TOKEN}
  subscribedEntityIds = ""
  subscribedEntityIds = ${?HOME_ASSISTANT_SUBSCRIBED_ENTITY_IDS}
  maxFrameSizeKb = 1024
  maxFrameSizeKb = ${?HOME_ASSISTANT_MAX_FRAME_SIZE_KB}
}
